import React, {Component} from 'react';
import {Text} from 'react-native';
import {withNavigation} from 'react-navigation';

export default withNavigation(class Logout extends Component {
    render() {
        return (
            <Text/>
        );
    }
});
